spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3307/study_room_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:3468580094}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:127.0.0.1}
      port: ${SPRING_DATA_REDIS_PORT:6380}
      database: 0

studyroom:
  jwt:
    secret: dev-secret-key-for-local-testing-only-change-in-production
    expiration: 86400000
